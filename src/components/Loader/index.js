import './index.css'

const Loader = () => { 
    return (
        <div className="loader">
            <div className="loader-logo loading">
                <div>A</div>
                <span>+</span>
            </div>
        </div>
    );
}

export default Loader;
